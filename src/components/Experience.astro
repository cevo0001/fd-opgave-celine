<section class="experience">
  <div class="section-inner">
    <div class="exp-inner">
      <!-- Venstre tekst -->
      <div class="exp-text">
        <h3>Experience</h3>
        <h2>More than 25 years of experience</h2>
        <p>3000+ our clients are insured around the World</p>
      </div>

      <!-- Højre cirkler -->
      <div class="exp-stats">
        <div class="stat" data-percent="85">
          <div class="circle">
            <span>85%</span>
          </div>
          <p>Consistency</p>
        </div>

        <div class="stat" data-percent="95">
          <div class="circle">
            <span>95%</span>
          </div>
          <p>Improvement</p>
        </div>

        <div class="stat" data-percent="90">
          <div class="circle">
            <span>90%</span>
          </div>
          <p>Branching</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @layer components {
    .experience {
      background-color: #ffcc00;
      color: #000;
      padding: 6rem 2rem;
    }

    .exp-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 3rem;
    }

    .exp-text {
      flex: 1 1 40%;
      text-align: left;
    }

    .exp-text h3 {
      text-transform: uppercase;
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
      letter-spacing: 1px;
    }

    .exp-text h2 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .exp-text p {
      font-size: 1rem;
      color: #222;
    }

    .exp-stats {
      flex: 1 1 50%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 2rem;
    }

    .stat {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.8rem;
    }

    /* Progress Circle */
    .circle {
      --value: 0deg;
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background: conic-gradient(#000 var(--value), #fff var(--value));
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    .circle::after {
      content: "";
      position: absolute;
      inset: 8px;
      background-color: #ffcc00;
      border-radius: 50%;
    }

    .circle span {
      position: relative;
      z-index: 1;
      font-size: 1.6rem;
      font-weight: 700;
    }

    .stat p {
      font-weight: 500;
      text-align: center;
      margin: 0;
    }

    /* Dynamic fill values */
    .stat[data-percent="85"] .circle {
      --value: 306deg;
    }
    .stat[data-percent="90"] .circle {
      --value: 324deg;
    }
    .stat[data-percent="95"] .circle {
      --value: 342deg;
    }
  }
</style>
